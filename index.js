class NormesOuvertes {
    static appliquerNorme(nomNorme) {
        // Logique pour utiliser la norme
        return `Données extraites pour la norme: ${nomNorme}`;
    }
}

module.exports = NormesOuvertes;